# Online Shopping Bill

# Input
customer_name = input("Enter Customer Name: ")
purchase_amount = float(input("Enter Purchase Amount: "))

# Calculate Discount
if purchase_amount >= 5000:
    discount = purchase_amount * 0.20   # 20%
elif purchase_amount >= 3000:
    discount = purchase_amount * 0.15   # 15%
elif purchase_amount >= 1000:
    discount = purchase_amount * 0.10   # 10%
else:
    discount = 0

# Amount after discount
amount_after_discount = purchase_amount - discount

# GST (18%)
gst = amount_after_discount * 0.18

# Final Bill
total_bill = amount_after_discount + gst

# Display Result
print("\n------ Online Shopping Bill ------")
print("Customer Name      :", customer_name)
print("Purchase Amount    : Rs.", purchase_amount)
print("Discount           : Rs.", discount)
print("Amount After Discount : Rs.", amount_after_discount)
print("GST (18%)          : Rs.", gst)
print("Total Bill         : Rs.", total_bill)
output:
------ Online Shopping Bill ------
Customer Name      : Priya
Purchase Amount    : Rs. 4000.0
Discount           : Rs. 600.0
Amount After Discount : Rs. 3400.0
GST (18%)          : Rs. 612.0
Total Bill         : Rs. 4012.0
